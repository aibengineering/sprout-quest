{
  "name": "sprout-quest",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun run build.ts",
    "test": "bun test",
    "balance": "bun run src/balance.ts",
    "typecheck": "tsc --noEmit",
    "art": "bash art/build.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
